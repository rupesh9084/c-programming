#include<stdio.h>

int main(){
int a;
long  b;
long long c;
double d;
long double e;
printf("the size of int %d\n",sizeof(a));

printf("the size of long  %d\n",sizeof(b));

printf("the size of long long %d\n",sizeof(c));

printf("the size of double %d\n",sizeof(d));

printf("the size of long double %d",sizeof(e));
return 0;
}