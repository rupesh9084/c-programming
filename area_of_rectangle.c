#include<stdio.h>

int main(){
int length,bridth,area;
printf("enter the length:");
scanf("%d",&length);
printf("enter the bridth:");
scanf("%d",&bridth);
area=length*bridth;
printf("the area of rectangle is:%d",area);
return 0;
}